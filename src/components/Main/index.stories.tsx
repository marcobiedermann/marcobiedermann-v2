/* eslint-disable import/no-extraneous-dependencies */

import { storiesOf } from '@storybook/react';
import React from 'react';
import Main from '.';

storiesOf('Main', module).add('default', () => <Main>Content</Main>);
