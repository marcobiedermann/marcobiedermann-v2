/* eslint-disable import/no-extraneous-dependencies */

import { storiesOf } from '@storybook/react';
import React from 'react';
import Header from '.';

storiesOf('Header', module).add('default', () => <Header>Content</Header>);
